using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Opration : MonoBehaviour
{
    public Rigidbody rigidbody;
    public GameObject GObject;
    public Vector3 V3;
    private void FixedUpdate() {
    //rigidbody.AddForceAtPosition(GObject.transform.position,GObject.transform.position,ForceMode.Force);    
    //rigidbody.WakeUp();
    }
}
